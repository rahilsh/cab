replicaCount: 1

image:
  repository: ghcr.io/rahilsh/cab
  tag: ""
  digest: ""
  pullPolicy: IfNotPresent
imagePullSecrets: []

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: false
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
  runAsGroup: 10001
  fsGroup: 10001
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  requireTls: true
  className: ""
  annotations: {}
  hosts:
    - host: cab.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

config:
  databaseUrl: jdbc:postgresql://postgresql:5432/cab
  redisHost: redis
  redisPort: "6379"
  oidcIssuerUri: https://identity.example.com/realms/cab
  oidcJwkSetUri: https://identity.example.com/realms/cab/protocol/openid-connect/certs
  oidcAudience: cab-api
  osrmBaseUrl: http://osrm:5000
  springProfilesActive: prod
  flywayEnabled: "false"
  apiDocsEnabled: "false"
  swaggerUiEnabled: "false"
  outboxDispatcherEnabled: "true"

# Create this Secret outside Helm. This chart never stores credential values.
existingSecret:
  name: ""
  databaseUsernameKey: database-username
  databasePasswordKey: database-password
  migrationDatabaseUsernameKey: migration-database-username
  migrationDatabasePasswordKey: migration-database-password
  fakePaymentWebhookSecretKey: fake-payment-webhook-secret

migration:
  enabled: true
  # The pre-install hook runs before chart-managed ServiceAccounts exist.
  serviceAccountName: default
  backoffLimit: 1
  activeDeadlineSeconds: 900
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: "1"
      memory: 1Gi

extraEnv: []

# Additional externally managed secrets, including tenant webhook signing secrets.
# Example: { name: CAB_WEBHOOK_PARTNER_A, secretName: cab-webhooks, key: partner-a }
extraSecretEnv: []

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi

probes:
  startup:
    failureThreshold: 30
    periodSeconds: 5
  liveness:
    initialDelaySeconds: 5
    periodSeconds: 10
  readiness:
    initialDelaySeconds: 5
    periodSeconds: 10

terminationGracePeriodSeconds: 30

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70

podDisruptionBudget:
  enabled: false
  minAvailable: 1

networkPolicy:
  enabled: false
  ingressNamespaceSelector: {}
  egress: []

nodeSelector: {}
tolerations: []
affinity: {}
